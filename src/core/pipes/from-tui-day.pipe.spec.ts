import { FromTuiDayPipe } from './from-tui-day.pipe';

describe('FromTuiDayPipe', () => {
  it('create an instance', () => {
    const pipe = new FromTuiDayPipe();
    expect(pipe).toBeTruthy();
  });
});
