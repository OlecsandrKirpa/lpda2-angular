import { AutosizeIframeDirective } from './autosize-iframe.directive';

describe('AutosizeIframeDirective', () => {
  it('should create an instance', () => {
    const directive = new AutosizeIframeDirective();
    expect(directive).toBeTruthy();
  });
});
